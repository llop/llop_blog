<div class="sidebar_box" style="margin:6px 10px 10px 0px;">
<div>
  <span>Categories</span>
  <hr/>
  <div class="sidebar_subbox">
  <% @categories.each do |category| %>
    <div class="menu_element">
    <%= link_to category.name, category_path(category.id) %>
    </div>
  <% end %>
  </div>
</div>
</div>